using System.Collections;
using System.Collections.Generic;
using System.Threading.Tasks;
using UnityEngine;

namespace YA
{
    public class CubeQTE : MonoBehaviour
    {
        // references to other scripts
        //[SerializeField] QuickTimeEvent QTE;
        //[SerializeField] InputScript inputScript;

        // gameObjects
        //public GameObject cubeQTE_OBJ;

        // at the start of the game
        //private void Start()
        //{

        //}

        // every frame
        //private void Update()
        //{

        //}

        // move the cubeQTE to the targetCube so the ontrigger event can happen
        //public async void MoveCubeQte()
        //{
            // while the loop is QTE is true do this:
            //while (QTE.Loop)
            //{
                // wait 1ms before doing something 
                //await Task.Delay(1);
                // move the cube
                //cubeQTE_OBJ.transform.position += new Vector3(0, 0, 0.012f);
            //}
        //}
    
    }
}